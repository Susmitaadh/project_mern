export enum Role {
  ADMIN = "ADMIN",
  USER = "USER",
  HOST = "HOST",
}

//* property price type
export enum ProprtyPriceType {
  "PER_HR" = "per hour",
  "PER_DAY" = "per day",
  "PER_WEEK" = "per week",
}

//* property type
export enum PropertyType {
  "ROOM" = "room",
  "FLAT" = "flat",
  "APARTMENT" = "apartment",
}
